# !/usr/bin/env bash
# -*- mode: shell-script-mode -*-

die () {
    >&2 echo "$1"
    exit 1
}

setp () {
    [[ -z "${1}" ]] && return 1
    return 0
}
setp "${JWT}" || die 'set $JWT'

curl -Ss -k -H "Authorization: ${JWT}" -H "Content-Type: application/json" -d @- "$@"
