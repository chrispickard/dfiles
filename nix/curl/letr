# !/usr/bin/env bash

die () {
    echo "$1"
    exit 1
}

setp () {
    [[ -z "${1}" ]] && return 1
    return 0
}
setp "${JWT}" || die 'set $JWT'

tee /dev/stderr | curl -Ss -k -H "Authorization: ${JWT}" -H "Content-Type: application/json" -d @- "$@"
