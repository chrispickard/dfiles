#!/usr/bin/env fish

set TMPL_LOC "$HOME/dfiles/tmpl"
if test ! (command -v "op")
    echo "op not found"
    exit 1
end

for i in $TMPL_LOC/.*
    if test (basename "$i") != ".gitkeep"
        cat "$i" | op inject --cache=false >~/(basename $i)
    end
end
